---
title: 'SVG'
layout: 'docs'
---

SVG's can be imported as a component in React.
Remember the extension `.component.svg` so the compiler understands it.

```jsx
import { ReactComponent as Logo } from './logo.component.svg';

function App() {
  return (
    <div>
      <Logo />
    </div>
  );
}
```

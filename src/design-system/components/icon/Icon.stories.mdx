import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { Button } from 'theme-ui';

import ThemeUIRenderer from '@/design-system/Theme-UI-renderer';

import Icon from "./Icon";
import {ReactComponent as ActivityIcon} from "@/design-system/icons/source/activity.svg";

<Meta
  title="Components/Icon"
  decorators={[
    // wrap these stories in the Theme UI provider
    (storyFn) => <ThemeUIRenderer>{storyFn()}</ThemeUIRenderer>
  ]}
  parameters={{
    controls: { hideNoControlsWarning: true }
  }}
/>

# Default

<Preview>
  <Story name="default">
    <Icon>
      <ActivityIcon/>
    </Icon>
  </Story>
</Preview>

### Icon Props

<Props of={Icon} />

# Custom size

<Preview>
  <Story name="Custom size">
    <Icon size={10}>
      <ActivityIcon/>
    </Icon>
  </Story>
</Preview>

# Custom color

<Preview>
  <Story name="Custom color">
    <Icon color="primary">
      <ActivityIcon/>
    </Icon>
  </Story>
</Preview>

# In a Button

<Preview>
  <Story name="In a Button">
    <Button>
      Show my activity
      <Icon sx={{ml: 2}}>
        <ActivityIcon/>
      </Icon>
    </Button>
  </Story>
</Preview>

# Using theme-ui props

<Preview>
  <Story name="Using theme-ui props">
    <Icon color="secondary" p={4}>
      <ActivityIcon/>
    </Icon>
  </Story>
</Preview>

# Using the SX prop

<Preview>
  <Story name="Using the SX prop">
    <Icon sx={{color: 'green.6'}}>
      <ActivityIcon/>
    </Icon>
  </Story>
</Preview>

import { Meta } from '@storybook/addon-docs/blocks';

import {LineHeights} from '../../../../.storybook/components/LineHeights';

import theme from '@/design-system/theme-ui.theme.ts';

<Meta
  title="Design Tokens/Typography/Line Heights"
  decorators={[
    // wrap these stories in the Theme UI provider
    (storyFn) => <ThemeUIRenderer theme={theme}>{storyFn()}</ThemeUIRenderer>
  ]}
/>

# Line Heights

<LineHeights
  fontFamily={theme.fonts.body}
  lineHeights={theme.lineHeights}
/>

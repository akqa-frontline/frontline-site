import { Meta } from '@storybook/addon-docs/blocks';

import {FontSizes} from '../../../../.storybook/components/FontSizes';

import theme from '@/design-system/theme-ui.theme.ts';

<Meta
  title="Design Tokens/Typography/Font Sizes"
  decorators={[
    // wrap these stories in the Theme UI provider
    (storyFn) => <ThemeUIRenderer theme={theme}>{storyFn()}</ThemeUIRenderer>
  ]}
/>

# Font Sizes

All font-sizes are responsive over 1 breakpoint.

<FontSizes
  fontSizes={theme.fontSizes}
/>
